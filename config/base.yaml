app:
  name: atlas
  environment: base
  debug: false

logging:
  level: INFO
  console: false
  json: true
  log_dir: logs

database:
  host: localhost
  port: 5432
  name: atlas

redis:
  host: localhost
  port: 6379

llm:
  default_model: gpt-4o
  temperature: 0.2
